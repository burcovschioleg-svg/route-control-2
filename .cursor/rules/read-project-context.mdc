---
description: Контекст и синхронизированные чаты Cursor
alwaysApply: true
---

# Route Control v2 — контекст

Пользователь — Oleg. Проект: `route_control_2`.

Перед большими задачами прочитай:

1. `docs/ALL_CHATS.txt` — **весь текст чатов** (главный файл).
2. `docs/CHAT_HISTORY.md` — смысл проекта.

v1 (`route_control`) — архив, не трогать без просьбы.

Конец сессии: предложи Oleg запустить `scripts/sync-chats.ps1` и `git push`.
