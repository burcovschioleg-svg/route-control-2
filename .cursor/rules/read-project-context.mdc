---
description: На старте чата читать контекст проекта и историю
alwaysApply: true
---

# Контекст Route Control v2

Пользователь — Oleg. Активный проект: `route_control_2`.

Перед большими задачами прочитай (если ещё не в контексте):

1. `docs/PROJECT_CONTEXT.md` — архитектура, eTOLL цепочка, правила.
2. `docs/CHAT_HISTORY.md` — полная история чатов (или последние сессии в конце файла).

v1 (`route_control`) — архив, не разрабатывать без явной просьбы.
